import React, {Component} from 'react';
import {IPosts} from "../../models/IPosts";

class PostComponent extends Component<IPosts> {

    componentDidMount() {}
    render() {
        const (id) =this.state
        return (
            <div>

                </div>
        );
    }
}

export default PostComponent;